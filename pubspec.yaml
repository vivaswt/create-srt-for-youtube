name: create_srt_for_youtube
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  deep_pick: ^1.1.0
  http: ^1.5.0
  youtube_explode_dart: ^2.5.2
  path: ^1.9.1
  path_provider: ^2.1.5
  mime: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - data/
